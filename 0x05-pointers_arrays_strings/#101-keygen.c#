#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
	int i, j, k = 0;
	char a[10], c[10], d;

	k = strlen(username);
	i = k - 1;
	j = 0;

	while (i >= 0)
	{
		c[j] = a[i] + i;
		i--;
		j++;
	}
	c[j] = 0;
}
