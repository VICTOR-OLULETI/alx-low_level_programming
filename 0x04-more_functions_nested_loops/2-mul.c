#include "main.h"

/**
 * mul - returns the multiplication of integer a and integer b
 * @a: a is the first integer
 * @b: b is the second integer
 *
 * Return: multiplication of int a and int b which is result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
