#include <stdio.h>

/**
 * main - program that prints its name followed by new line
 *
 * @argc: argc is the number of arguments
 * @argv: argv is the array character
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
